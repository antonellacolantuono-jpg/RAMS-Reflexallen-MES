{
  "name": "@mes/domain",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "xstate": "^5.18.0"
  },
  "devDependencies": {
    "@mes/config": "workspace:^",
    "@mes/types": "workspace:*",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
