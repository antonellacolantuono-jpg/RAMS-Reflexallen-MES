{
  "name": "@mes/prisma",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && tsc",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx seed.ts",
    "seed:pneumatic": "tsx seed/pneumatic.ts",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset --force",
    "generate": "prisma generate",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^5.21.0",
    "argon2": "^0.44.0"
  },
  "devDependencies": {
    "@mes/config": "workspace:*",
    "@types/node": "^20.14.10",
    "prisma": "^5.21.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
