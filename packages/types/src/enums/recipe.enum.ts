export enum RecipeStatus {
  DRAFT = 'draft',
  APPROVED = 'approved',
  DEPRECATED = 'deprecated',
}

export enum RecipeParameterType {
  NUMERIC = 'numeric',
  STRING = 'string',
  ENUM = 'enum',
  BOOLEAN = 'boolean',
}
