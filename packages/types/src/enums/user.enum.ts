export enum UserRole {
  ADMIN = 'admin',
  PLANNER = 'planner',
  SUPERVISOR = 'supervisor',
  QUALITY = 'quality',
  OPERATOR = 'operator',
  VIEWER = 'viewer',
}

export enum OperatorStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}
